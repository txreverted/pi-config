---
description: Run one internal writer followed by two fresh read-only reviews
argument-hint: "<implementation objective>"
---
Run the `implement-review` workflow for this explicitly authorized implementation objective:

$ARGUMENTS

The workflow may modify the current checkout. Use exactly one writer, preserve unrelated user changes, and report deterministic verification plus any confirmed review findings.
