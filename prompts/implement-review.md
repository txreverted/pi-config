---
description: Delegate implementation, verify it, then request one fresh read-only review
argument-hint: "<implementation objective>"
---
Implement this authorized change in the current checkout:

$ARGUMENTS

Clarify material product ambiguity before delegation. Call `subagent` once with a sole foreground `worker` task containing the objective and acceptance criteria. Then inspect its diff, preserve unrelated user changes, and rerun deterministic checks yourself. Call `subagent` once with the `reviewer` role for an independent read-only pass. Verify and address only confirmed findings. Report changed files and checks.
