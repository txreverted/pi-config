---
description: Rebuild and replace documentation, including dirty files
argument-hint: "[scope]"
---
Rebuild docs. Scope: ${ARGUMENTS:-entire repository}. Dirty in-scope replacement needs no confirmation.

Read applicable `AGENTS.md`. Classify tracked/untracked/dirty Markdown owner/status. Protect instructions, runtime prompts/policies, generated/frozen files, licenses/notices, ignored/vendor content, unrelated changes. Old docs are leads, not evidence, for owner/marker/voice/example facts; verify via code/config/tests/contracts/safe output.

Uppercase Markdown basenames; lowercase `.md`. Rename files/references. Prepare all replacements before writes/deletes; name dirty docs replaced. Keep root `README.md`; add task docs only if burdened. Write drafts, then delete only obsolete in-scope human docs. Edit Markdown only.

README: title; 1-3 exact opening sentences; top instruction links; shortest safe setup/run/check commands. Use/change/verify/troubleshoot facts only. Put limits/side effects with behavior. Prefer bullets under 80 lines; tables only to compare. Use exact paths/verified commands and source/test links. Omit placeholders, inventories, version/CI tables, history/roadmap, implementation narration.

Only required/documented Markdown checks. No paid calls/deploys/migrations/pushes/publishes/live operations. Verify claims/commands/paths/links/examples. Report created/updated/deleted docs, doc/code mismatches; omit unchanged.
